<Window x:Class="MonkeyUSStockViewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MonkeyUSStockViewer"
        mc:Ignorable="d"
        Title="Monkey US Stock Viewer - Settings" Height="780" Width="1080"
        Closing="Window_Closing">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="170" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="110" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="110" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Margin="0,0,8,8" VerticalAlignment="Center" Text="App Key" />
            <TextBox x:Name="AppKeyTextBox" Grid.Column="1" Margin="0,0,16,8" />

            <TextBlock Grid.Column="2" Margin="0,0,8,8" VerticalAlignment="Center" Text="App Secret" />
            <PasswordBox x:Name="AppSecretPasswordBox" Grid.Column="3" Margin="0,0,0,8" />
        </Grid>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="110" />
                <ColumnDefinition Width="120" />
                <ColumnDefinition Width="110" />
                <ColumnDefinition Width="120" />
                <ColumnDefinition Width="120" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Margin="0,0,8,8" VerticalAlignment="Center" Text="Interval Sec" />
            <TextBox x:Name="PollingIntervalTextBox" Grid.Column="1" Margin="0,0,16,8" />

            <TextBlock Grid.Column="2" Margin="0,0,8,8" VerticalAlignment="Center" Text="Font Size" />
            <TextBox x:Name="TickerFontSizeTextBox" Grid.Column="3" Margin="0,0,16,8" />

            <CheckBox x:Name="AlwaysOnTopCheckBox" Grid.Column="4" Margin="0,0,16,8" VerticalAlignment="Center" Content="Always On Top" />
            <TextBlock x:Name="StatusTextBlock" Grid.Column="5" Margin="0,0,0,8" VerticalAlignment="Center" Text="Idle" />
        </Grid>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="110" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="110" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Margin="0,0,8,8" VerticalAlignment="Center" Text="Token URL" />
            <TextBox x:Name="AccessTokenUrlTextBox" Grid.Column="1" Margin="0,0,16,8" />

            <TextBlock Grid.Column="2" Margin="0,0,8,8" VerticalAlignment="Center" Text="Price URL" />
            <TextBox x:Name="PriceDetailUrlTextBox" Grid.Column="3" Margin="0,0,0,8" />
        </Grid>

        <Grid Grid.Row="3" Margin="0,4,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="360" />
                <ColumnDefinition Width="16" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <GroupBox Grid.Column="0" Header="Stocks">
                <ListBox x:Name="StocksListBox" Margin="8" SelectionChanged="StocksListBox_SelectionChanged" />
            </GroupBox>

            <GroupBox Grid.Column="2" Header="Stock Editor">
                <Grid Margin="8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="120" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Margin="0,0,8,8" VerticalAlignment="Center" Text="Display Name" />
                    <TextBox x:Name="StockDisplayNameTextBox" Grid.Row="0" Grid.Column="1" Margin="0,0,16,8" />

                    <TextBlock Grid.Row="0" Grid.Column="2" Margin="0,0,8,8" VerticalAlignment="Center" Text="Symbol" />
                    <TextBox x:Name="StockSymbolTextBox" Grid.Row="0" Grid.Column="3" Margin="0,0,0,8" />

                    <TextBlock Grid.Row="1" Grid.Column="0" Margin="0,0,8,8" VerticalAlignment="Center" Text="Market" />
                    <ComboBox x:Name="StockMarketComboBox" Grid.Row="1" Grid.Column="1" Margin="0,0,16,8" IsEditable="True" SelectionChanged="StockMarketComboBox_SelectionChanged">
                        <ComboBoxItem Content="NASDAQ" />
                        <ComboBoxItem Content="NYSE" />
                        <ComboBoxItem Content="AMEX" />
                    </ComboBox>

                    <TextBlock Grid.Row="1" Grid.Column="2" Margin="0,0,8,8" VerticalAlignment="Center" Text="Mode" />
                    <ComboBox x:Name="ExchangeModeComboBox" Grid.Row="1" Grid.Column="3" Margin="0,0,0,8" IsEditable="False">
                        <ComboBoxItem Content="Auto" />
                        <ComboBoxItem Content="Day" />
                        <ComboBoxItem Content="Regular" />
                        <ComboBoxItem Content="Manual" />
                    </ComboBox>

                    <TextBlock Grid.Row="2" Grid.Column="0" Margin="0,0,8,8" VerticalAlignment="Center" Text="Day Code" />
                    <ComboBox x:Name="DayExchangeCodeComboBox" Grid.Row="2" Grid.Column="1" Margin="0,0,16,8" IsEditable="True">
                        <ComboBoxItem Content="BAQ" />
                        <ComboBoxItem Content="BAY" />
                        <ComboBoxItem Content="BAA" />
                    </ComboBox>

                    <TextBlock Grid.Row="2" Grid.Column="2" Margin="0,0,8,8" VerticalAlignment="Center" Text="Regular Code" />
                    <ComboBox x:Name="RegularExchangeCodeComboBox" Grid.Row="2" Grid.Column="3" Margin="0,0,0,8" IsEditable="True">
                        <ComboBoxItem Content="NAS" />
                        <ComboBoxItem Content="NYS" />
                        <ComboBoxItem Content="AMS" />
                    </ComboBox>

                    <TextBlock Grid.Row="3" Grid.Column="0" Margin="0,0,8,8" VerticalAlignment="Center" Text="Manual Code" />
                    <TextBox x:Name="ManualExchangeCodeTextBox" Grid.Row="3" Grid.Column="1" Margin="0,0,16,8" />

                    <TextBlock Grid.Row="3" Grid.Column="2" Margin="0,0,8,8" VerticalAlignment="Center" Text="Qty" />
                    <TextBox x:Name="HoldingQuantityTextBox" Grid.Row="3" Grid.Column="3" Margin="0,0,0,8" />

                    <TextBlock Grid.Row="4" Grid.Column="0" Margin="0,0,8,8" VerticalAlignment="Center" Text="Avg Price" />
                    <TextBox x:Name="AveragePriceTextBox" Grid.Row="4" Grid.Column="1" Margin="0,0,16,8" />

                    <StackPanel Grid.Row="5" Grid.Column="0" Grid.ColumnSpan="4" Orientation="Horizontal">
                        <Button Width="90" Margin="0,0,8,0" Click="AddStockButton_Click" Content="Add" />
                        <Button Width="90" Margin="0,0,8,0" Click="UpdateStockButton_Click" Content="Update" />
                        <Button Width="90" Margin="0,0,8,0" Click="DeleteStockButton_Click" Content="Delete" />
                        <Button Width="90" Click="ClearStockButton_Click" Content="Clear" />
                    </StackPanel>
                </Grid>
            </GroupBox>
        </Grid>

        <StackPanel Grid.Row="4" Margin="0,0,0,12" Orientation="Horizontal">
            <Button x:Name="SaveButton" Width="110" Margin="0,0,8,0" Click="SaveButton_Click" Content="Save" />
            <Button x:Name="StartPollingButton" Width="120" Margin="0,0,8,0" Click="StartPollingButton_Click" Content="Start Polling" />
            <Button x:Name="StopPollingButton" Width="110" Margin="0,0,16,0" Click="StopPollingButton_Click" Content="Stop" IsEnabled="False" />
            <CheckBox x:Name="LogRawJsonCheckBox" Margin="0,0,16,0" VerticalAlignment="Center" Content="Log raw JSON" />
            <TextBlock Margin="0,0,8,0" VerticalAlignment="Center" Text="Token" />
            <TextBox x:Name="AccessTokenTextBox" Width="320" IsReadOnly="True" />
        </StackPanel>

        <TextBox x:Name="RawLogTextBox"
                 Grid.Row="5"
                 AcceptsReturn="True"
                 FontFamily="Consolas"
                 FontSize="12"
                 HorizontalScrollBarVisibility="Auto"
                 IsReadOnly="True"
                 TextWrapping="NoWrap"
                 VerticalScrollBarVisibility="Auto" />
    </Grid>
</Window>
